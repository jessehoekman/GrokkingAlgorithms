class Solution(object):
    def twoSum(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """

        if (nums[i] + nums[x]) == target
            return [nums[i].index, nums[x].index]
        else

target = 9
nums = [2,7,11,15] 